import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

const CustomInvestAccordionTopDownBox = styled(Box)(({ theme }) => ({
    marginTop: '3rem',
    columnGap: '3rem',
  }));

export default CustomInvestAccordionTopDownBox;
